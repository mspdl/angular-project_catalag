import { Routes } from '@angular/router';
import { ConverterComponent } from './components/converter.component';

export const CurrencyConverterRoutes: Routes = [
  { path: 'currency_converter', component: ConverterComponent },
];
